spring:
  datasource:
    url: jdbc:mysql://localhost:3306/alloservicedb?createDatabaseIfNotExist=true
    username: root
    password: 1qaz2wsx@
    driver-class-name: com.mysql.jdbc.Driver
    
  server:
  port: 9090 
  
  jpa:
    hibernate:
      ddl-auto: create

  security:
    oauth2:
      resource:
        token-info-uri: http://localhost:8191/oauth/check_token
      client:
        client-id: web
        client-secret: web
        
  
          
        